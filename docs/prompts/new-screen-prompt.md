Create a new screen using the shared app standards.

Requirements:

- Use shared button, text input, form, loading, and error components.
- Keep styling consistent with existing screens.
- Use feature services rather than direct backend calls in the screen.
- Add analytics for screen view and primary action.
- Handle loading, empty, success, and failure states.
- Prevent duplicate submission.
- Support keyboard-safe and mobile-friendly behavior.
- If the screen is gated, use the shared RevenueCat entitlement check.

Deliver:

- screen component
- supporting hook/service changes
- analytics event additions
- notes for manual QA
